1 - This is not like the asynchronous run within the experiments. It is a new corrected one. Indeed, when writing the paper, I made sure that the implementation is correct. 
so, when checking the grid browsing I found that I am using the old browsing. Knowing that I suspected that the browsing is corrupted, I wanted to avoid any miscunducting, so
I corrected the code and run again.